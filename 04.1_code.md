<<<<<<< HEAD
**Imputing Missing Values with Mean**
======================================

Here's an example of how you can impute missing values in a Pandas DataFrame with the mean of the column:
=======
**Imputing Missing Values in a Pandas DataFrame with the Mean of the Column**
====================================================================

Below is a Python implementation that imputes missing values in a Pandas DataFrame with the mean of the column.
>>>>>>> 271491f530511bdc50d214f6e52a42bda8e670b9

```python
# Import necessary libraries
import pandas as pd
import numpy as np

<<<<<<< HEAD
# Function to impute missing values with the mean of the column
def impute_missing_values_with_mean(df):
    """
    Impute missing values in a Pandas DataFrame with the mean of the column.

    Parameters:
    df (pd.DataFrame): DataFrame with missing values.

    Returns:
    pd.DataFrame: DataFrame with missing values imputed.
=======
def impute_missing_values(df):
    """
    Imputes missing values in a Pandas DataFrame with the mean of the column.

    Args:
        df (pd.DataFrame): Input DataFrame.

    Returns:
        pd.DataFrame: DataFrame with missing values imputed.
>>>>>>> 271491f530511bdc50d214f6e52a42bda8e670b9
    """
    # Create a copy of the input DataFrame to avoid modifying it in-place
    imputed_df = df.copy()

    # Iterate over each column in the DataFrame
<<<<<<< HEAD
    for col in df.columns:
        # Check if the column contains numeric values
        if pd.api.types.is_numeric_dtype(df[col]):
            # Calculate the mean of the column, excluding missing values
            mean_value = df[col].mean()
            # Impute missing values with the mean of the column
            df[col] = df[col].fillna(mean_value)
        else:
            # For non-numeric columns, print a message indicating that they will not be imputed
            print(f"Column '{col}' is not numeric and will not be imputed.")
    
    # Return the DataFrame with missing values imputed
    return df
=======
    for column in imputed_df.columns:
        # Check if the column contains any missing values
        if imputed_df[column].isnull().any():
            # Calculate the mean of the column, excluding missing values
            column_mean = imputed_df[column].mean()

            # Replace missing values in the column with the calculated mean
            imputed_df[column] = imputed_df[column].fillna(column_mean)

    return imputed_df
>>>>>>> 271491f530511bdc50d214f6e52a42bda8e670b9

# Example usage
if __name__ == "__main__":
    # Create a sample DataFrame with missing values
    data = {
<<<<<<< HEAD
        "A": [1, 2, np.nan, 4, 5],
        "B": [np.nan, 2, 3, 4, 5],
        "C": ["a", "b", np.nan, "d", "e"]  # Non-numeric column
    }
    df = pd.DataFrame(data)

    # Print the original DataFrame
    print("Original DataFrame:")
    print(df)

    # Impute missing values with the mean of the column
    df_imputed = impute_missing_values_with_mean(df)

    # Print the DataFrame with missing values imputed
    print("\nDataFrame with missing values imputed:")
    print(df_imputed)
```

**Code Explanation**
-------------------

1. The `impute_missing_values_with_mean` function takes a Pandas DataFrame `df` as input and iterates over each column.
2. For numeric columns, it calculates the mean of the column (excluding missing values) using `df[col].mean()`.
3. It then imputes missing values with the mean of the column using `df[col].fillna(mean_value)`.
4. For non-numeric columns, it prints a message indicating that they will not be imputed.
5. Finally, the function returns the DataFrame with missing values imputed.

**Example Use Case**
--------------------

The provided example creates a sample DataFrame `df` with missing values and demonstrates how to use the `impute_missing_values_with_mean` function to impute missing values. The output shows the original DataFrame and the DataFrame with missing values imputed.
=======
        'A': [1, 2, np.nan, 4, 5],
        'B': [np.nan, 2, 3, 4, 5],
        'C': [1, 2, 3, np.nan, 5]
    }
    df = pd.DataFrame(data)

    print("Original DataFrame:")
    print(df)

    # Impute missing values in the DataFrame
    imputed_df = impute_missing_values(df)

    print("\nDataFrame with missing values imputed:")
    print(imputed_df)
```

**Explanation:**

*   The `impute_missing_values` function takes a Pandas DataFrame as input and returns a new DataFrame with missing values imputed.
*   It iterates over each column in the input DataFrame and checks for missing values using the `isnull()` method.
*   If a column contains missing values, it calculates the mean of the column using the `mean()` method, excluding missing values.
*   It then replaces missing values in the column with the calculated mean using the `fillna()` method.
*   Finally, it returns the DataFrame with missing values imputed.

**Note:** This implementation assumes that the DataFrame contains only numeric columns. If the DataFrame contains non-numeric columns, you may need to modify the implementation accordingly.

**Example Use Case:**

*   The example usage demonstrates how to create a sample DataFrame with missing values and impute them using the `impute_missing_values` function.
*   The output shows the original DataFrame with missing values and the DataFrame with missing values imputed.
>>>>>>> 271491f530511bdc50d214f6e52a42bda8e670b9
